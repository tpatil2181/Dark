//6. Create an enum called myEnum, with 2 constants (myFirstConst,
//mySecondConst) with the string values "first" and "second":

enum myEnum {
    myFirstConst = "first",
    mySecondConst = "second",
  }

  const firstValue = myEnum.myFirstConst;   
const secondValue = myEnum.mySecondConst; 

console.log(firstValue);  
console.log(secondValue); 
